# Google Cloud → reCAPTCHA Enterprise → Create key (Web) → pegar site key
# Luego registrarla en Firebase → App Check → app web → reCAPTCHA Enterprise
VITE_FIREBASE_APPCHECK_SITE_KEY=

# Solo desarrollo (localhost). true = ver token en consola y registrarlo en App Check
# VITE_FIREBASE_APPCHECK_DEBUG_TOKEN=true
